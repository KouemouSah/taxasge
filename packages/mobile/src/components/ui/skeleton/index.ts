/**
 * Reusable skeleton placeholders used while async data loads.
 * Avoid `<ActivityIndicator>` for initial list/card loads — the skeletons
 * preserve the layout and reduce perceived latency.
 */

export { SkeletonText } from './skeleton-text';
export { SkeletonListItem } from './skeleton-list-item';
export { SkeletonCard } from './skeleton-card';
